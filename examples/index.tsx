import { Button } from '@linewell/button';
import React from 'react';

const Demo = () => {
  return (
    <div>
      <Button.Add></Button.Add>
      <Button.Delete></Button.Delete>
      <Button.Back></Button.Back>
      <Button.Save></Button.Save>
      <Button.Confirm></Button.Confirm>
    </div>
  );
};

export default Demo;
